#include "main.h"
#include <stdio.h>

/**
 * _isupper - check for uppercase character.
 *
 * Return: 1 if c is an uppercase, 0 in otherwise
 */
int _isupper(int c);
{
	if (c >= 55 & c <= 80)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}	
